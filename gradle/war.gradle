apply plugin: "war"

bootWar {
    mainClass = "com.github.sisi.CourseEnrollBackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
